--[[
    Plugin Name: customcalls
    Creator: PrizonPony
    Description: Implements custom CAD calls
]]
CreateThread(function() Config.LoadPlugin("customcalls", function(pluginConfig)

if pluginConfig.enabled then

end
end) end)